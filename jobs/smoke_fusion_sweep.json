{
  "name": "smoke_fusion_sweep",
  "datasets": ["hcrl_ch"],
  "training_types": ["fusion"],
  "sweep": {
    "fusion_agent.fusion_batch_size": [64, 256],
    "fusion_agent.target_update_freq": [10, 100]
  },
  "extra_args": {
    "epochs": 1
  },
  "notes": "Example small sweep manifest for smoke fusion runs (use with oscjobmanager preview/submit)."
}
