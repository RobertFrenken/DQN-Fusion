{
  "name": "pipeline_vgae_gat_fusion",
  "datasets": "all",
  "training_types": ["autoencoder", "curriculum", "fusion"],
  "extra_args": {
    "epochs": 5,
    "batch_size": 16
  },
  "dependency_manifests": {
    "default": "/project/manifests/{dataset}_manifest.json"
  }
}