/* Light-theme overrides for article/paper context */

export {
    COLORS,
    STATUS_COLORS,
    LABEL_COLORS,
    MODEL_COLORS,
    CONFIG_COLORS,
    DATASET_COLORS,
    METRIC_COLORS,
    semanticColorScale,
    colorScale,
} from './Theme.js';

/** Light-theme axis/grid styling overrides */
export const LIGHT_OVERRIDES = {
    axisColor: '#374151',
    gridColor: '#e5e7eb',
    textColor: '#1f2937',
    background: '#ffffff',
};

/** Tighter margins for print/article context */
export const MARGIN = { top: 16, right: 24, bottom: 44, left: 52 };
